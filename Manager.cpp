#include "Manager.h"

Manager::Manager(const std::string& SSN, const std::string& firstName, const std::string& lastName,
    const std::string& dateOfBirth)
    : Person(SSN, firstName, lastName, dateOfBirth) {}
